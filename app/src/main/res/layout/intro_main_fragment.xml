<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@color/colorPrimary"
    android:layout_height="wrap_content">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/intro_fragment_view_pager"
        android:layout_width="match_parent"
        android:layout_above="@id/intro_fragment_page_indicator"
        android:layout_height="match_parent"/>



    <me.relex.circleindicator.CircleIndicator
        android:id="@+id/intro_fragment_page_indicator"
        android:layout_width="match_parent"
        android:layout_above="@id/intro_fragment_text_view"
        android:layout_height="48dp"/>
    <TextView
        android:id="@+id/intro_fragment_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/welcome"
        android:textColor="@android:color/white"
        android:textSize="@dimen/very_big_text_size"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        />
</RelativeLayout>