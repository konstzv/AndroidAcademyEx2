<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:background="@color/colorPrimary"
    android:layout_height="wrap_content">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/intro_activity_view_pager"
        android:layout_width="match_parent"
        android:layout_above="@id/intro_activity_page_indicator"
        android:layout_height="match_parent"/>



    <me.relex.circleindicator.CircleIndicator
        android:id="@+id/intro_activity_page_indicator"
        android:layout_width="match_parent"
        android:layout_above="@id/intro_activity_text_view"
        android:layout_height="48dp"/>
    <TextView
        android:id="@+id/intro_activity_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome"
        android:textColor="@android:color/white"
        android:textSize="40sp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        />
</RelativeLayout>